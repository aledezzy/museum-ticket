<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Reindirizza le richieste per la directory radice al file index.php
    RewriteRule ^ app/public/index.php [L]   
    
    # Reindirizza tutte le altre richieste non corrispondenti ad altri file o directory al file index.php
    #RewriteCond %{REQUEST_FILENAME} !-f
    #RewriteCond %{REQUEST_FILENAME} !-d
    #RewriteRule ^ /app/public/index.php [L]
</IfModule>